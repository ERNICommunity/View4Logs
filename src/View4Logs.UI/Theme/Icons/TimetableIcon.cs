using System.Windows;

namespace View4Logs.UI.Theme.Icons
{
    public sealed class TimetableIcon : System.Windows.Controls.Control
    {
        static TimetableIcon()
        {
            DefaultStyleKeyProperty.OverrideMetadata(typeof(TimetableIcon), new FrameworkPropertyMetadata(typeof(TimetableIcon)));
        }
    }
}