using System.Windows;

namespace View4Logs.UI.Theme.Icons
{
    public sealed class FilterIcon : System.Windows.Controls.Control
    {
        static FilterIcon()
        {
            DefaultStyleKeyProperty.OverrideMetadata(typeof(FilterIcon), new FrameworkPropertyMetadata(typeof(FilterIcon)));
        }
    }
}