using System.Windows;

namespace View4Logs.UI.Theme.Icons
{
    public sealed class EyeIcon : System.Windows.Controls.Control
    {
        static EyeIcon()
        {
            DefaultStyleKeyProperty.OverrideMetadata(typeof(EyeIcon), new FrameworkPropertyMetadata(typeof(EyeIcon)));
        }
    }
}
