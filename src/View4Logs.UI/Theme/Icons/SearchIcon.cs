using System.Windows;

namespace View4Logs.UI.Theme.Icons
{
    public sealed class SearchIcon : System.Windows.Controls.Control
    {
        static SearchIcon()
        {
            DefaultStyleKeyProperty.OverrideMetadata(typeof(SearchIcon), new FrameworkPropertyMetadata(typeof(SearchIcon)));
        }
    }
}